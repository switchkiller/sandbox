
#include<bits/stdc++.h>

using namespace std;

#define REP(i,a,b) for (int i = (a); i < (b); i++)
#define RREP(i,a,b) for (int i = (a); i > (b); i--)
#define FOR(i,a,b) for (int i = (a); i <= (b); i++)

int n, a, b, p, q;
int main(){
    cin >> n >> a >> b >> p >> q;
    // XXX Contd easy problem - handle the large test cases R -> 10^9
    int x = n / a;
    int y = n / b;
    int z = n / (a)

    return 0;
}
