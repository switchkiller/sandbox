// XXX incomplete #use dp #segment tree #binaryindexing

#include<bits/stdc++.h>

using namespace std;

#define REP(i,a,b) for (int i = (a); i < (b); i++)


int main(){
	int N;
	cin >> N;
	int val[N-1];
	REP(i,0,N-1)
		cin >> val[i]; 
	

	return 0;
}