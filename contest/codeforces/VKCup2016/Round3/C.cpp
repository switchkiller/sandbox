// C. Levels and Regions - #dp #probability #greedy #dnc
#include<bits/stdc++.h>

using namespace std;

#define REP(i,a,b) for (int i = (a); i < (b); i++)
#define RREP(i,a,b) for (int i = (a); i > (b); i--)

int main(){
    int n, k;
    cin >> n >> k;
    int a[n];
    REP(i,0,n) cin >> a[i];





  return 0;
}
