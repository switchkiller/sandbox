// Pending 
#include<bits/stdc++.h>

using namespace std;

#define REP(i,a,b) for (int i = (a); i < (b); i++)
#define RREP(i,a,b) for (int i = (a); i > (b); i--)
#define FOR(i,a,b) for (int i = (a); i <= (b); i++)
#define inf 0x3f3f3f3f
#define ll long long
#define mp make_pair
#define pb push_back
#define LSOne(x) x&(-x)
#define debug(x) cout << #x << " " << x << endl;
#define all(X) X.begin(), X.end()

typedef vector<int> vi;
typedef pair<int, int> ii;
typedef vector<ii> vii;

int dp[101][101];
int dx[] = { 1, 0 };
int dy[] = { 0, 1 };

vector<int> tokenize( string a, string b){
    const char *q = a.c_str();
    while(count ( all(b), *q ) )
        q++;
    vector< string > oot;
    while ( *q ){

    }

}

class AvoidRoads{
    public:
        long long numWays(int width, int height, vector <string> bad){
            set< pair< ii, ii > > nt;
            REP(i,0,bad.size()){
                vi st = sti( tokenize( bad[i], " "));
            }


      }


      }

};

int main(){

    return 0;
}
